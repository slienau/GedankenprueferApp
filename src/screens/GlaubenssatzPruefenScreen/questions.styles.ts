import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    // paddingVertical: 20,
    // paddingTop: 0,
  },
  inputTextContainer: {
    marginTop: 20,
  },
  inputTextStyle: {
    minHeight: 150,
  },
  questionHeader: {
    paddingBottom: 10,
  },
});

export default styles;
