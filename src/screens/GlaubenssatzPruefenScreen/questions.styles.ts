import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    paddingVertical: 20,
  },
  inputTextContainer: {
    marginTop: 20,
  },
  inputTextStyle: {
    minHeight: 150,
  },
});

export default styles;
